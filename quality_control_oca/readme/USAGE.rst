Excel Import Workflow
=====================

Use the *Import Tests from Excel* wizard to load new or updated quality tests
from the provided spreadsheet template. The wizard validates the file, shows a
preview, and lets you choose whether to only add new tests or also update
existing ones.

Documentation
-------------

For detailed column definitions, sample data, and the latest download link,
refer to :doc:`import_excel_template`.
